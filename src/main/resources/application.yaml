spring:
  application:
    name: ytSupportAgent
langchain4j:
  open-ai:
    chat-model:
      api-key: ${OPEN_API_KEY}
      model-name: gpt-4o
      temperature: 0.7
      max-tokens: 1000
camel:
  component:
    vertx-websocket:
      default-port: 8080